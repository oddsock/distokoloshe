aiohttp>=3.9.0
# Pinned: >=1.1.3 bundles a Rust FFI binary that requires key_ring_size and
# key_derivation_function in E2EE KeyProviderOptions, but the Python SDK's
# Room.connect() never sets them (bug in livekit-rtc). This causes the music
# bot to fail to connect with E2EE enabled. Unpin once the SDK is fixed:
# https://github.com/livekit/python-sdks/issues/570
livekit==1.1.2
yt-dlp[default]>=2025.5.22
